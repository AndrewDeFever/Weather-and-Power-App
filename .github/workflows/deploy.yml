name: "Deploy Weather-and-Power App (SAM)"

on:
  push:
    branches: ["main"]
  workflow_dispatch: {}

concurrency:
  group: "weather-power-status-deploy"
  cancel-in-progress: true

permissions:
  id-token: write
  contents: read

env:
  SAM_CLI_TELEMETRY: "0"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout"
        uses: actions/checkout@v4

      - name: "Configure AWS credentials (OIDC)"
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: "Verify AWS identity"
        run: |
          aws sts get-caller-identity
          aws configure list

      - name: "Set up Python"
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: "Install dependencies (checks)"
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install -r requirements-dev.txt

      - name: "Fail-fast: syntax + import checks"
        run: |
          python -m compileall -q .
          # Minimal ruff ruleset: catch syntax errors, undefined names, and similar deploy-breakers.
          ruff check --select=E9,F63,F7,F82 .

      - name: "Run tests"
        run: |
          PYTHONPATH=. pytest -q tests

      - name: "Install AWS SAM CLI"
        uses: aws-actions/setup-sam@v2

      - name: "Guard: wait for stack to be stable"
        run: |
          set -euo pipefail
          STATUS=$(aws cloudformation describe-stacks --stack-name weather-power-status --query "Stacks[0].StackStatus" --output text 2>/dev/null || echo "MISSING")
          echo "Stack status: $STATUS"

          if [[ "$STATUS" == "MISSING" ]]; then
            echo "Stack does not exist yet; continuing."
            exit 0
          fi

          if [[ "$STATUS" == *"IN_PROGRESS"* ]]; then
            echo "Stack is busy ($STATUS). Waiting..."
            aws cloudformation wait stack-update-complete --stack-name weather-power-status || true
            aws cloudformation wait stack-create-complete --stack-name weather-power-status || true
          fi

          STATUS2=$(aws cloudformation describe-stacks --stack-name weather-power-status --query "Stacks[0].StackStatus" --output text)
          echo "Stack status after wait: $STATUS2"

          if [[ "$STATUS2" == *"IN_PROGRESS"* ]]; then
            echo "Stack still in progress; failing to avoid collision."
            exit 1
          fi

      - name: "SAM Build"
        run: sam build --debug

      - name: "SAM Validate"
        run: sam validate

      - name: "SAM Deploy"
        run: |
          sam deploy \
            --stack-name weather-power-status \
            --region us-east-1 \
            --s3-bucket weathernpowerapp \
            --capabilities CAPABILITY_IAM CAPABILITY_NAMED_IAM \
            --role-arn arn:aws:iam::042889969210:role/weather-power-status-cfn-exec \
            --no-confirm-changeset \
            --no-fail-on-empty-changeset \
            --debug

      # --- Deploy frontend to S3 (wnp.subrealstudios.com) ---
      # Expects these files to exist in the repo:
      #   frontend/index.html
      #   frontend/static/app.js
      #   frontend/static/styles.css
      - name: "Deploy frontend (S3 static website)"
        shell: bash
        env:
          FRONTEND_BUCKET: wnp.subrealstudios.com
        run: |
          set -euo pipefail

          # Fail fast if the frontend isn't present (prevents silent drift)
          test -f frontend/index.html
          test -f frontend/static/app.js
          test -f frontend/static/styles.css

          echo "Uploading static assets (no-cache to avoid stale JS during rapid iteration)..."
          aws s3 sync frontend/static "s3://${FRONTEND_BUCKET}/static" \
            --delete \
            --cache-control "no-cache, no-store, must-revalidate"

          echo "Uploading index.html (no-cache)..."
          aws s3 cp frontend/index.html "s3://${FRONTEND_BUCKET}/index.html" \
            --cache-control "no-cache, no-store, must-revalidate" \
            --content-type "text/html"

      - name: "Dump CloudFormation events on failure"
        if: failure()
        run: |
          aws cloudformation describe-stack-events \
            --stack-name weather-power-status \
            --max-items 50

      - name: "Continue rollback only if needed"
        if: failure()
        run: |
          STATUS=$(aws cloudformation describe-stacks --stack-name weather-power-status --query "Stacks[0].StackStatus" --output text)
          echo "Current stack status: $STATUS"
          if [[ "$STATUS" == "UPDATE_ROLLBACK_FAILED" || "$STATUS" == "UPDATE_ROLLBACK_IN_PROGRESS" ]]; then
            aws cloudformation continue-update-rollback --stack-name weather-power-status
          else
            echo "No rollback action required."
          fi

      - name: "Show stack status"
        run: |
          aws cloudformation describe-stacks \
            --stack-name weather-power-status \
            --query "Stacks[0].StackStatus" \
            --output text || true

      - name: "Tail recent stack events"
        run: |
          aws cloudformation describe-stack-events \
            --stack-name weather-power-status \
            --max-items 30 \
            --query "StackEvents[].[Timestamp,ResourceStatus,LogicalResourceId,ResourceType,ResourceStatusReason]" \
            --output table || true
